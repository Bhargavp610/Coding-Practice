-- Approach: Queried all rows and columns directly using SELECT *
SELECT * FROM CITY;

-- City by ID
-- Approach: Queried all columns but restricted result to a specific ID using WHERE
SELECT * FROM CITY
WHERE ID = '1661';